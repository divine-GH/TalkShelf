# Python
__pycache__/
*.pyc
.venv/
.ruff_cache/

# 环境与密钥（key 不进 git，设计文档 §9）
.env

# 数据库与本地数据
*.db
*.db-wal
*.db-shm
.ollama/
data/
!backend/app/data/

# 测试临时目录
.pytest-tmp/
.pytest_cache/
pytest-cache-files-*/
nb-test-*/
.nb-smoke-e2e-*/
# 遗留的 ACL 损坏目录（os.mkdir(0o700) 产物，无法在沙箱内删除，等待手动清理）
_probe700/

# M4 部署（Cloudflare Tunnel 方案，见 deploy/README.md）：二进制与隧道 token 不入库
deploy/cloudflared/cloudflared.exe
deploy/cloudflared/tunnel-token.txt
deploy/logs/
deploy/*.pid
