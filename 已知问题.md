# 已知问题（Known Issues）

> 记录已确认存在的问题及其影响面、触发条件与恢复路径。
> 状态含义：
> - `待修复` = 已确认缺陷，修复已排期（按重要度依次修复，每项经用户验收后继续下一项）；
> - `暂不修复` = 已知缺陷但当前可接受（有降级/恢复路径、不丢数据），修复未排期；
> - `待评估` = 已记录，优先级低，是否修复待评估；
> - `已修复` = 用户验收通过；条目标注修复提交（未发版时注明），发版时同步 CHANGELOG。
>
> 新增条目模板见文末。

## 问题清单

### #1 没装 Ollama 但「本地 Embedding」开关开启 → 直存笔记永久卡 pending、向量化空转重试

- **状态**：暂不修复（2026-08-20 记录，用户决定；修复未排期）
- **类别**：错误配置组合下的行为缺陷（仅影响「开关开着但机器上没有可用 Ollama」的机器）

#### 触发条件（缺一不可）

1. `EMBEDDING_ENABLED=1`（`.env` 配置）或设置页「本地 Embedding」开关为开（DB 设置覆盖 .env 默认值）；
2. 本机 `127.0.0.1:11434` 没有可用的 Ollama（未安装 / serve 未运行 / bge-m3 模型缺失）。

> 默认配置（`EMBEDDING_ENABLED` 默认 0，§35）**不触发**本问题——这正是总开关默认关的原因。

#### 现象

1. **快捷入口直存的新笔记永久卡 `pending`**：管线顺序为 整理 → embedding → 查重 → FTS（§14 第 6 条），
   embedding 抛 `EmbeddingError` 会中断整条 `_process`，退避重试 60s/5m/15m/1h/6h 共 5 次后
   **保持原状态、不标 failed**（§14 第 8 条语义：等恢复后补）。于是笔记永远不转 `processed`、
   永远不查重，回顾页一直显示「未决策/进行中」；每次应用重启，启动扫描又提交一轮，重复 5 次退避。
2. **对话式记录/普通落库的笔记不受状态影响，但缺向量**：落库时已置 `processed`、FTS 已同步，
   检索/详情正常；只是每次启动扫描都会尝试补向量并空转 5 次退避。
3. **问答页降级 FTS-only**：每次提问向量路连接 `127.0.0.1:11434` **立即失败**（本机无监听，
   不会等 30s 超时），记一条日志后降级关键词检索，界面提示「语义检索暂不可用」；
   回答仍可用（FTS 关键词 + 材料层兜底 + LLM 作答）。
4. **日志噪音**：启动扫描 + 每次提问 + 每轮退避都会产生 warning/error 日志。

#### 不影响

- 不崩溃、不丢数据、不阻塞请求（向量失败均在异步队列/降级路径内消化）；
- 对话式记录、链接抓取、问答（降级）、回顾、详情、统计、登录全部可用；
- 关键词检索（FTS5 trigram）、材料层兜底召回、LLM 作答不受影响。

#### 根因

`EMBEDDING_ENABLED=1` 的语义是「本机声明有可用 Ollama」；此时向量化是管线硬步骤，
失败即中断整条管线（宁可保持状态重试，也不跳过向量继续 FTS+查重——跳过意味着永久放弃
向量化，与 §14 第 8 条「恢复后补 embedding」语义冲突）。对「没装 Ollama」这种**永久性**
场景，正确配置是关掉开关（§35），而非依赖重试兜底。

#### 恢复路径（无需修复代码）

1. **装 Ollama + `ollama pull bge-m3`**：下次启动扫描自动补向量 → 查重 → 转 `processed`；
2. **关开关**（设置页或 `.env` 设 `EMBEDDING_ENABLED=0`）后重启应用：启动扫描重新提交
   卡住的 `pending` 笔记，跳过向量步、走 FTS 近似查重并正常转 `processed`；
3. 已卡笔记也可用详情页「重新整理」（reprocess）手动触发。

#### 将来可能的修复方向（仅记录，不承诺）

- 向量失败时跳过向量步、继续 FTS+查重+状态推进，向量改为「尽力而为」待办而非管线中断点——
  与 §14 第 8 条现有语义冲突，需先重新设计状态机（如 pending 推进但保留向量待办标记）；
- 启动时探测 Ollama 可用性，不可用时自动视同开关关闭，避免每轮空转重试。

#### 相关代码/文档

- `backend/app/config.py` L114-116：`EMBEDDING_ENABLED` 默认关
- `backend/app/queue.py`：L105-111（embedding 失败不标 failed）、L143-158（向量步）、L55-75（启动扫描）
- `backend/app/dedup.py` L70-82：查重退化 FTS 近似版
- `backend/app/retrieval.py` L171-185：检索降级 FTS-only
- `start.ps1` L13-25：未检测到 Ollama 时跳过拉起并提示
- 设计文档 §35（总开关决策记录）、§14 第 8 条（Ollama 单点降级策略）

---

### #2 用户消息在会话区重复渲染（检索页 + 记录页同根因）🔴

- **状态**：已修复（提交 447bd64，未发版）——P1，2026-08-25 排查，用户验收通过
- **类别**：前端模板渲染缺陷（必现）
- **触发条件**：检索页/记录页提交任意一条用户消息。
- **现象**：聊天区出现两条完全相同的用户气泡。证据（检索会话 #16）：DB `messages` 仅 5 行
  （user 消息只插入一次），历史列表显示「5 条消息」，但页面 DOM 为 6 个 `.msg` 节点。
- **根因**：
  - `templates/ask.html` 提交成功后 `openChatFor()`（`lastMsgId` 归 0）→ 手动
    `appendMsg('user','text',msg)` → `pollReply()` 首轮 `renderMessages()` 把 `id > 0` 的
    全部消息再追加一遍，刚落库的用户消息被重复渲染；追问路径同理。
  - `templates/chat.html` 提交处理手动 `appendMsg('user','text',msg)` → POST 落库（新 id =
    lastMsgId+1）→ `pollReply()` 以 `id > lastMsgId` 追加 → 同一条用户消息第二次渲染。
- **修复**：删除两页提交处理中的手动 `appendMsg('user','text',msg)`，用户消息统一由轮询渲染
  （消息 id 追踪成为唯一渲染来源）；`makeThinking()` 保留，「思考中…」即时出现。
- **相关代码/文档**：`templates/ask.html`、`templates/chat.html`；快照「检索 — TalkShelf.html」（工作区根，非仓库）

### #3 来源 URL 携带追踪参数入库/展示 🟠

- **状态**：已修复（提交 d10167d / 5f4a5fa，未发版）——P2，2026-08-25 排查，用户验收通过
- **类别**：数据卫生 + 隐私（抓取链）
- **触发条件**：抓取「分享短链」类来源（如 B 站 `b23.tv`）后，检索材料命中该笔记。
- **现象**：笔记 13 原始内容为干净短链 `https://b23.tv/VbGsdko`，但 `note_materials.url` 存的是
  重定向长链：`…?buvid=…&from_spmid=…&mid=…&share_session_id=…&timestamp=…&unique_k=…&up_id=…`，
  并作为「命中于来源材料」的「来源 ↗」链接展示。buvid（设备指纹）、mid（用户标识）、
  share_session_id 均为 B 站 App 分享追踪参数。
- **根因**：抓取时 httpx 跟随重定向，最终 URL（含全部追踪参数）写入 `Fetched <url>` 头；
  材料 URL 按设计从该头提取并落库、展示。
- **修复方向**：抓取记录**原始请求 URL**，或对常见追踪参数（`buvid`/`mid`/`share_*`/`spmid`/
  `from_spmid`/`unique_k`/`timestamp` 等）清洗后再写入材料 URL；需同时覆盖 `Fetched <url>` 头
  与 `note_materials.url` 两处并保持既有测试契约。
- **已实施修复**：`fetch.strip_tracking_url()` 在 `fetch.fetched_message` 源头应用——Fetched 头、
  消息 url 字段、`note_materials.url`、「来源 ↗」链接与 LLM 上下文一次全部清洗；页面路径与
  功能参数（如 `?p=1`、小黑盒 `link_id`）保留。清洗策略（B+A 组合，用户 2026-08-25 拍板）：
  - **B 通用黑名单**：站点专属（`buvid`/`mid`/`spmid`/`from_spmid`/`unique_k`/`timestamp`/
    `up_id`/`plat_id`/`h_camp`/`h_src`/`from`/`referer`/`ref`/`share_*`）+ 业界通用广告归因
    参数（`gclid`/`fbclid`/`yclid`/`igshid`/`msclkid`/`mc_*`/`_hs*`/`spm`/`scm` 等 +
    `utm_*`/`pk_*`/`mtm_*` 前缀）；
  - **A 站点白名单**：`bilibili.com`（仅留 `p`）、`b23.tv`（无参数）、`xiaoheihe.cn`（仅留
    `link_id`，功能参数），命中站点其余全剥（站内新增埋点参数即时防漏）；未知站点退回 B。
  - 已知边界：非白名单站点若出现新的站点专属追踪参数，依赖 B 的黑名单覆盖；已落库历史脏
    URL 不回填（如需清理另行处理——涉及改库数据，需用户确认）。
- **相关代码/文档**：`backend/app/fetch.py`（重定向/`Fetched` 头）、材料 URL 提取路径（数据模型 §4）

### #4 检索材料框近重复 🟠

- **状态**：已修复（提交 e57a4ac，未发版）——P3，2026-08-25 排查，用户验收通过
- **类别**：检索会话展示冗余（§36）
- **触发条件**：LLM 在收到自动检索注入后仍以近同概念调用 `note_search`。
- **现象**：同一轮问答出现 3 个「检索材料」折叠框：自动注入（query=原问题）、LLM 工具重查
  （query=「Эндшпиль」，来源集合仅差 2 条、视觉雷同）、LLM 二次重查（不同关键词、6 条无材料）；
  答案引用 `[1][7]` 只对应第 1 个框编号，用户难以对号。
- **根因**：`_process_search_round` 每轮自动注入 search_hits；LLM 工具循环的 `note_search` 结果
  （`_run_note_search` 追加 tool_events）也全部落库展示；前端每个 search_hits 消息独立渲染，
  无同质去重/折叠。上下文侧无膨胀（`_search_llm_messages` 只注入最近一条）。
- **修复方向**：工具结果与最近一条 search_hits 同质（query 相同或 sources 集合相同）则跳过落库，
  或展示层折叠/标注；亦可 prompt 提示「已注入，重查请用不同关键词」。
- **已实施修复**（归组而非去重，数据与追溯完整保留）：
  - 前端 `ask.html`：同一轮连续的多个 search_hits 合并进**一个**「检索材料」折叠条，每块标题
    带各自 query（`「…」`），折叠条标题汇总为「检索材料（共 N 条来源 · M 次检索）」；非结构化
    旧数据按文本块展示；样式 `.hit-block`/`.hit-query`（static/style.css）。跨轮次（中间有
    用户消息/材料）不合并。
  - prompt 引导（llm.py 规则 2）：「每轮自动检索结果已注入，若已覆盖请勿重复同概念检索；
    补检/重检请用不同关键词」——减少同词重查（也省 LLM 往返与时延）。
  - 遗留说明：答案 `[n]` 编号只对应自动注入块；补充检索块独立编号不参与答案引用（与 P7
    口径问题一起处理）。
- **相关代码/文档**：`backend/app/api.py`（`_process_search_round`）、`backend/app/llm.py`
  （`_run_note_search`）、`templates/ask.html`（`buildHitsBlock`/`appendHitsBox`）

### #5 材料命中片段质量差（导航噪声打头、原始 markdown 展示）🟠

- **状态**：待修复——P4
- **类别**：抓取正文质量 + 检索展示
- **触发条件**：抓取含站点导航的页面（如 B 站移动页）后，材料层命中该笔记。
- **现象**：材料命中片段以页面标题 + `* [首页](//www.bilibili.com)` 等导航菜单开头；UI 的
  `note-summary` 原样显示原始 markdown（不渲染为链接），且不定位到命中词。
- **根因**：抓取正文（markdownify）保留导航块并落入 `note_materials.text`；检索 snippet 按文本头
  截取窗口；展示层纯文本渲染 markdown。
- **修复方向**：抓取正文裁剪常见导航/页脚块；snippet 选取定位命中词的回溯窗口；展示层做受限
  markdown 渲染（仅链接/加粗，白名单 XSS 防护）。
- **相关代码/文档**：`backend/app/fetch.py`、`backend/app/retrieval.py`、`templates/ask.html`

### #6 轮询出错后「思考中…」气泡残留 🟡

- **状态**：待修复——P5（2026-08-25 用户决定留待后续）
- **类别**：前端状态清理
- **现象/根因**：`ask.html`/`chat.html` 的 `pollReply()` catch 分支只 `stopPolling()` +
  `showError()`，未清理思考气泡；错误后不再轮询，气泡永久停留（直到重开会话）。
- **修复方向**：catch 分支清理思考气泡。
- **相关代码/文档**：`templates/ask.html`、`templates/chat.html`

### #7 `backToList()` 状态残留 🟡

- **状态**：待修复——P6（2026-08-25 用户决定留待后续）
- **类别**：前端状态清理
- **现象/根因**：`ask.html` 返回列表不清 `#messages`、`#chat-id-label`、`lastMsgId`；隐藏区残留
  旧会话 DOM（快照保存时恰好抓到该状态）。
- **修复方向**：`backToList()` 清空消息区与会话标签，复位 `lastMsgId`。
- **相关代码/文档**：`templates/ask.html`

### #8 计数口径与用户感知不一致 🟢

- **状态**：待评估——P7（2026-08-25 用户决定留待后续，含 P3 遗留的「答案引用编号仅对应自动注入块」说明）
- **类别**：UX 口径
- **现象**：历史列表「N 条消息」= `COUNT(*)`，把检索材料/搜索/抓取消息也算作「消息」；
  `buildHitsBox`「N 条来源」= sources + material_sources，与答案引用编号 `[n]`（仅 sources 计号、
  材料追加在尾）需要自行对位。
- **修复方向**：列表按「问答 N 轮」口径展示；材料框编号标注与答案引用一致。低优先级。
- **相关代码/文档**：`backend/app/api.py`（`list_search_conversations`）、`templates/ask.html`

---

## 新增条目模板

```markdown
### #N 标题
- **状态**：暂不修复 / 已修复（vX.Y.Z）
- **类别**：
- **触发条件**：
- **现象**：
- **根因**：
- **影响**：
- **恢复路径**：
- **相关代码/文档**：
```

约定：编号连续递增，不复用已用编号；修复后把状态改为「已修复（vX.Y.Z）」保留条目
（并在 CHANGELOG 记录），或删除条目并在 CHANGELOG 注明。
